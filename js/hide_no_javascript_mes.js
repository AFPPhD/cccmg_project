$(document).ready(function() { // Use jQuery(function() {});

    $('.nojavascript').remove();   // remove action makes element disappear

	$('.clickbandtext').show();    // show click band for info text

	// $('.navigation h3.hidden, .navigation ul.hidden').removeClass('hidden'); //Don't actually want to hide navigation menu
	// when javascript disabled. Want to allow once click functionality. Screen readers ???
});